# Create an empty list to store the input values
my_list = []

# Get the number of elements in the list from the user
num_elements = int(input("Enter the number of elements: "))

# Iterate through the range of the number of elements
for i in range(num_elements):
    # Prompt the user for input and add it to the list
    element = input("Enter element {}: ".format(i+1))
    my_list.append(element)

# Print the list
print("The list you entered is:", my_list)
